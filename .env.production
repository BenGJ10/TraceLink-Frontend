# Production template — safe to commit (no real secrets here)
# Before deploying, create a real .env.production with the actual values
# and inject it during your CodeBuild/CI step.

VITE_API_BASE_URL=https://api.tracelink.bengregoryjohn.in
